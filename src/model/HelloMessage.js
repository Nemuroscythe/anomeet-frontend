// Cette classe permet de préciser le type d'objet javascript reçu,
// elle n'est pas obligatoire mais permet de voir facilement les variables de l'objet
class HelloMessage {
    constructor(content) {
        this.content = content
    }
}